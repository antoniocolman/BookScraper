# BookSearchV2

CLI maestro + plugins de sitios + storage (SQLite) + exports.

## Quick start
1) Crear venv
2) Instalar requirements
3) Ejecutar comandos:
   - python -m app sites
   - python -m app analyze-url --url "https://example.com/product"
